 - Commitment mapper
  - Test performance with Validator where bytes are just array of bits or when they are more dense
  - Write down how exactly it works
 - Start VerifyAttestationData
   - The attestations satisfy all validation criteria from process_attestation
   - each validator is part of the active validator set through a merkle proof against the current BeaconState hash_tree_root and
   - that it belongs to the correct sub-committee.

